---
const { tags = [{}] } = Astro.props;
---

<div class="tags">
    {
        tags.map((tag: { backgroundColor: any; color: any; name: unknown }) => (
            <span class="tag" style={{ backgroundColor: tag.backgroundColor, color: tag.color }}>
                {tag.name}
            </span>
        ))
    }
</div>

<style>
    .tags {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 0.2rem;
    }
    .tag {
        border-radius: 10px;
        background-color: rgba(255, 127, 80, 0.479);
        padding: 0 0.4rem;
        min-width: 2rem;
        height: fit-content;
        font-size: 0.7rem;
        text-align: center;
        text-wrap: nowrap;
    }
</style>
